# 🔍 Проверка локальной машины Windows

## ⚠️ Да, майнер МОГ перебраться с вашего компьютера!

### Возможные сценарии:

1. **SSH ключи скомпрометированы**
   - Майнер/троян украл ваш SSH приватный ключ
   - Использует его для подключения к серверу

2. **Mайнер на локальной машине**
   - Заражён ваш Windows компьютер
   - Распространяется через SSH соединения

3. **Ключлоггер**
   - Записывает ваш пароль SSH
   - Использует его для взлома сервера

4. **Автоматическое распространение**
   - Майнер автоматически сканирует известные хосты
   - Пытается подключиться через украденные ключи

## 🔍 Проверка Windows машины

### Шаг 1: Проверьте процессы Windows

```powershell
# Откройте PowerShell от имени администратора

# Проверьте подозрительные процессы
Get-Process | Where-Object {$_.CPU -gt 50 -or $_.WorkingSet -gt 100MB} | Select-Object Name, CPU, WorkingSet, Path

# Проверьте процессы с подозрительными именами
Get-Process | Where-Object {$_.Name -match "miner|crypto|xmrig|stratum"} | Select-Object Name, Path, StartTime

# Проверьте сетевые соединения
netstat -ano | findstr "ESTABLISHED"
# Если видите подозрительные IP - это может быть майнер
```

### Шаг 2: Проверьте автозапуск Windows

```powershell
# Проверьте автозапуск
Get-CimInstance Win32_StartupCommand | Select-Object Name, Command, Location

# Проверьте планировщик задач
Get-ScheduledTask | Where-Object {$_.State -eq "Running"} | Select-Object TaskName, TaskPath

# Проверьте реестр автозапуска
Get-ItemProperty HKLM:\Software\Microsoft\Windows\CurrentVersion\Run
Get-ItemProperty HKCU:\Software\Microsoft\Windows\CurrentVersion\Run
```

### Шаг 3: Проверьте SSH ключи

```powershell
# Перейдите в директорию SSH ключей
cd ~/.ssh

# Проверьте ключи
ls -Force

# Проверьте содержимое authorized_keys (если есть)
cat authorized_keys 2>$null

# Проверьте когда последний раз изменялись ключи
Get-ChildItem ~/.ssh | Select-Object Name, LastWriteTime, LastAccessTime

# Проверьте SSH config
cat config 2>$null
```

### Шаг 4: Проверьте заражённые файлы

```powershell
# Поиск подозрительных файлов
Get-ChildItem -Path C:\ -Recurse -Filter "*miner*" -ErrorAction SilentlyContinue | Select-Object FullName
Get-ChildItem -Path C:\ -Recurse -Filter "*xmrig*" -ErrorAction SilentlyContinue | Select-Object FullName
Get-ChildItem -Path C:\ -Recurse -Filter "*crypto*" -ErrorAction SilentlyContinue | Select-Object FullName

# Проверьте временные файлы
Get-ChildItem -Path $env:TEMP -Filter "*miner*" -ErrorAction SilentlyContinue
Get-ChildItem -Path $env:TEMP -Filter "*583*" -ErrorAction SilentlyContinue
```

### Шаг 5: Проверьте антивирусом

```powershell
# Запустите полное сканирование
# Windows Defender:
Start-MpScan -ScanType FullScan

# Или используйте Malwarebytes, Kaspersky и т.д.
```

### Шаг 6: Проверьте историю SSH подключений

```powershell
# Если используете OpenSSH на Windows, проверьте логи:
Get-Content C:\ProgramData\ssh\logs\sshd.log -Tail 100 -ErrorAction SilentlyContinue

# Проверьте историю команд PowerShell
Get-History | Select-Object -Last 50

# Проверьте историю cmd (если есть)
Get-Content $env:APPDATA\Microsoft\Windows\PowerShell\PSReadline\ConsoleHost_history.txt -Tail 100
```

## 🔒 Что делать если майнер найден на Windows

### 1. Отключите интернет СРАЗУ

```powershell
# Отключите сеть
Disable-NetAdapter -Name "*" -Confirm:$false
```

### 2. Смените ВСЕ SSH ключи

```powershell
# Удалите старые SSH ключи
Remove-Item ~/.ssh/id_* -Force

# Создайте новые ключи
ssh-keygen -t ed25519 -C "new-key-$(Get-Date -Format 'yyyyMMdd')" -f ~/.ssh/id_ed25519_new

# Удалите старые ключи с сервера
# Зайдите на сервер через веб-консоль reg.ru и выполните:
# nano ~/.ssh/authorized_keys
# Удалите старую запись, добавьте новую
```

### 3. Удалите майнер

```powershell
# Найдите и убейте процесс
Get-Process | Where-Object {$_.Name -match "miner"} | Stop-Process -Force

# Найдите и удалите файл
Remove-Item -Path "НАЙДЕННЫЙ_ПУТЬ" -Force -Recurse

# Очистите автозапуск
Get-ScheduledTask | Where-Object {$_.TaskName -match "miner"} | Unregister-ScheduledTask -Confirm:$false
```

### 4. Проверьте сервер

После очистки Windows:
- Удалите старые SSH ключи с сервера
- Добавьте новый ключ
- Проверьте что майнер не вернулся

## 🎯 Признаки что майнер на Windows

- **Высокая загрузка CPU** когда ничего не делаете
- **Высокая температура** процессора
- **Медленная работа** компьютера
- **Подозрительные процессы** в диспетчере задач
- **Непонятные сетевые соединения**
- **Высокое использование интернета**

## ✅ План действий

1. **Проверьте Windows сейчас** - выполните команды выше
2. **Если майнер найден** - отключите интернет и удалите
3. **Смените SSH ключи** - старые больше не безопасны
4. **Очистите сервер** - удалите майнер и старые ключи
5. **Установите антивирус** - защитите Windows
6. **Настройте firewall** на Windows

## 🔍 Проверка на сервере: откуда подключался майнер

На сервере выполните:

```bash
# Проверьте SSH логи - с каких IP подключались
grep "Accepted publickey" /var/log/auth.log | tail -50

# Проверьте с каким ключом подключались
grep "Accepted publickey" /var/log/auth.log | awk '{print $11, $13}' | sort | uniq -c

# Проверьте когда майнер запустился
ps -p 1771 -o lstart,etime,cmd
ls -la /proc/1771/exe 2>/dev/null

# Проверьте файл майнера
find / -name "583af269" 2>/dev/null
ls -la /НАЙДЕННЫЙ_ПУТЬ/583af269
```

**Если в логах видите ваш IP - майнер точно с вашего компьютера!**

