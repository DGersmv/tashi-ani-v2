# 🔍 Расследование: Как проник майнер?

## Возможные пути проникновения

### 1. Через веб-приложение (Next.js)
- Уязвимости в коде приложения
- Загруженные файлы через API
- SQL injection (маловероятно с Prisma)
- File upload уязвимости

**Проверьте:**
```bash
# Логи Nginx
tail -1000 /var/log/nginx/access.log | grep -E "(POST|PUT|DELETE)" | grep -v "/api/auth"
tail -1000 /var/log/nginx/error.log

# Логи приложения
pm2 logs tashi-ani --lines 1000 | grep -i "error\|upload\|file"
```

### 2. Через SSH (если всё же был слабый пароль или ключ скомпрометирован)
- Брутфорс атака
- Компрометация SSH ключа

**Проверьте:**
```bash
# Логи авторизации
tail -1000 /var/log/auth.log | grep -E "(Failed|Accepted)"
grep "Failed password" /var/log/auth.log | wc -l
grep "Accepted publickey" /var/log/auth.log

# С каких IP подключались
grep "Accepted" /var/log/auth.log | awk '{print $11}' | sort | uniq -c | sort -rn
```

### 3. Через уязвимости в Node.js/npm пакетах
- Зараженные зависимости
- Устаревшие версии пакетов с уязвимостями

**Проверьте:**
```bash
cd /var/www/tashi-ani
npm audit
npm audit fix

# Какие пакеты установлены
npm list --depth=0
```

### 4. Через систему управления reg.ru
- Уязвимости в панели управления
- Компрометация через веб-консоль
- Доступ через API reg.ru

**Проверьте:**
```bash
# Кто заходил через веб-консоль
journalctl -u vncserver 2>/dev/null
journalctl | grep -i "console\|vnc\|web"
```

### 5. Через незакрытые уязвимости в системе
- Не обновлённые пакеты
- Уязвимости в ядре Linux
- Уязвимости в системных сервисах

**Проверьте:**
```bash
# Когда последний раз обновлялась система
ls -la /var/lib/apt/periodic/update-success-stamp

# Есть ли обновления
apt list --upgradable

# Уязвимости в системе
apt audit 2>/dev/null || echo "apt audit не установлен"
```

### 6. Через PM2 или другие процессы
- Компрометация через PM2
- Зараженные скрипты запуска

**Проверьте:**
```bash
# История PM2
pm2 list
pm2 describe tashi-ani

# Какие скрипты запускались
cat ecosystem.config.js
cat package.json | grep -A 10 "scripts"
```

### 7. Через файлы проекта (если клонировали с зараженного репозитория)
- Зараженные файлы в git
- Скрипты в node_modules
- Pre-install/post-install скрипты в package.json

**Проверьте:**
```bash
cd /var/www/tashi-ani

# Когда последний раз обновлялся проект
git log --oneline -10

# Что было изменено
git log --all --full-history -- "**/583af269**" "**/*miner*" "**/*crypto*"

# Проверьте package.json на подозрительные скрипты
cat package.json | grep -E "(postinstall|preinstall|install)" | grep -v "prisma generate"
```

### 8. Через автозапуск (cron/systemd)
- Задачи в crontab
- Systemd сервисы
- Автозапуск в скриптах

**Проверьте:**
```bash
# Crontab задачи
crontab -l
cat /etc/crontab
ls -la /etc/cron.d/
ls -la /etc/cron.daily/
ls -la /etc/cron.hourly/

# Systemd сервисы
systemctl list-units --type=service --all | grep -i "unknown\|suspicious"
ls -la /etc/systemd/system/ | grep -v "^d"
ls -la /usr/lib/systemd/system/ | grep -i "miner\|crypto"

# Проверьте автозапуск
cat /etc/rc.local 2>/dev/null
cat ~/.bashrc | grep -v "^#"
cat ~/.bash_profile 2>/dev/null | grep -v "^#"
```

### 9. Через временные файлы или загрузки
- Временные файлы в /tmp
- Загруженные файлы через сайт
- Файлы в /var/tmp

**Проверьте:**
```bash
# Что в /tmp
ls -la /tmp/ | grep -E "(583|miner|crypto|xmrig)"

# Что в директории загрузок
ls -la /var/www/tashi-ani/public/uploads/ 2>/dev/null | head -20

# Файлы с правами на выполнение
find /tmp /var/tmp -type f -executable 2>/dev/null
```

### 10. Майнер был с самого начала (неполная переустановка)
- Старые разделы диска не были очищены
- Данные в /home или других разделах
- Остались старые файлы

**Проверьте:**
```bash
# Когда была последняя переустановка
uptime
ls -la /root/ | head -20

# Проверьте все разделы диска
df -h
lsblk

# История системы
journalctl --since "2025-01-01" | head -100
```

## 🔍 Сценарий расследования

### Шаг 1: Найдите файл майнера

```bash
# Найдите файл процесса
ps aux | grep 583af269
# Запишите PID и путь

# Найдите файл
find / -name "583af269" 2>/dev/null
find /tmp /var/tmp -name "*583*" 2>/dev/null

# Проверьте откуда он запустился
ls -la /proc/1771/exe 2>/dev/null
cat /proc/1771/cmdline 2>/dev/null | strings
```

### Шаг 2: Проверьте когда появился

```bash
# Когда создан файл
ls -la /НАЙДЕННЫЙ_ПУТЬ/583af269

# История файла
stat /НАЙДЕННЫЙ_ПУТЬ/583af269

# Логи системы за этот период
journalctl --since "2025-01-07 01:00" --until "2025-01-07 02:00"
```

### Шаг 3: Проверьте откуда скачался

```bash
# Проверьте сетевые соединения
netstat -tulpn | grep 1771
ss -tulpn | grep 1771

# Куда майнер подключается
lsof -p 1771 | grep ESTABLISHED

# DNS запросы
cat /etc/resolv.conf
```

### Шаг 4: Проверьте как запустился

```bash
# Родительский процесс
ps -ef | grep 1771

# История процессов
pstree -p 1771

# Проверьте автозапуск (см. п.8 выше)
```

### Шаг 5: Проверьте логи веб-приложения

```bash
cd /var/www/tashi-ani

# Логи PM2
pm2 logs tashi-ani --lines 5000 | grep -E "(error|upload|file|POST|PUT)"

# Логи Nginx
tail -5000 /var/log/nginx/access.log | grep -E "(POST|PUT)" | grep -v "/api/auth/login"
tail -5000 /var/log/nginx/error.log

# Проверьте загруженные файлы
find public/uploads -type f -newer /var/www/tashi-ani/prisma/prod.db 2>/dev/null
```

## 🎯 Наиболее вероятные сценарии

### Сценарий 1: Через SSH (брутфорс)
**Признаки:**
- Много "Failed password" в /var/log/auth.log
- Подключения с разных IP
- Пароль был простой или fail2ban не работал

**Решение:** Только SSH ключи + whitelist IP

### Сценарий 2: Через веб-приложение
**Признаки:**
- Подозрительные POST/PUT запросы в логах Nginx
- Загруженные файлы в public/uploads
- Ошибки в логах приложения

**Решение:** Проверить код на уязвимости, валидацию загрузок

### Сценарий 3: Через npm пакеты
**Признаки:**
- npm audit показывает уязвимости
- Подозрительные скрипты в package.json
- Неизвестные процессы после npm install

**Решение:** npm audit fix, проверить package.json

### Сценарий 4: Неполная переустановка
**Признаки:**
- Файлы в /tmp или других разделах
- Старые логи или файлы
- Uptime показывает что система не переустанавливалась

**Решение:** Полная переустановка с форматированием диска

## ✅ Что делать сейчас

1. **Выполните все команды из раздела "Шаг 1-5"**
2. **Соберите результаты в файл**
3. **Проанализируйте откуда появился майнер**
4. **Устраните уязвимость ДО переустановки**
5. **Иначе майнер вернётся снова!**

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**Если не найдёте источник заражения - майнер вернётся после переустановки!**

Поэтому сначала расследуйте, потом переустанавливайте.

